import { minidenticon } from 'https://cdn.jsdelivr.net/npm/minidenticons@4.2.1/minidenticons.min.js';

(function setupIcon() {
	//const manageItem = document.querySelector('#manage-li');

	//if (manageItem === null)
	//	return;

	//const manageAnchor = manageItem.querySelector('#manage');
	
	//manageItem.removeChild(manageAnchor);

	//manageItem.insertAdjacentHTML('afterbegin', minidenticon("laurent", 95, 45));
	//const userIcon = manageItem.querySelector('svg');
	//const shapeDefs = [...userIcon.childNodes];
	//manageAnchor.replaceChildren(...shapeDefs);
	//userIcon.appendChild(manageAnchor);
})();
